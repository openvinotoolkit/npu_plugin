# -*- Cmake -*-

set(kernel_src "singleShaveInterpolate.cpp")

set(optimization_opts "") # -O3
set(always_inline "yes") # "no"

if(NOT always_inline STREQUAL "yes")
  set(extra_src_list "${KERNEL_DIR}/common/src/mvSubspaces.cpp")
endif()
