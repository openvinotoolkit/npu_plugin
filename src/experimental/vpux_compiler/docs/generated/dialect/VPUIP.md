<!-- Autogenerated by mlir-tblgen; don't manually edit -->
# 'VPUIP' Dialect

VPU NN RunTime Dialect
The `VPUIP` Dialect represents NN RunTime IR in terms of MLIR framework.

[TOC]

## Type constraint definition

### VPUIP Barrier
This object represents closely a Barrier in the device.

## Operation definition

### `VPUIP.ConfigureBarrier` (vpux::VPUIP::ConfigureBarrierOp)

A task to configure the setup for a barrier


Syntax:

```
operation ::= `VPUIP.ConfigureBarrier` attr-dict
              (`inputs` `(` $inputTensors^ `:` type($inputTensors) `)`)?
              (`outputs` `(` $outputTensors^ `:` type($outputTensors) `)`)?
              (`waits` `(` $waitBarriers^ `:` type($waitBarriers) `)`)?
              (`updates` `(` $updateBarriers^ `:` type($updateBarriers) `)`)?
              `->` type(results)
```


#### Operands:

| Operand | Description |
| :-----: | ----------- |
`inputTensors` | memref of any type values
`outputTensors` | memref of any type values
`waitBarriers` | VPUIP Barrier
`updateBarriers` | VPUIP Barrier

#### Results:

| Result | Description |
| :----: | ----------- |
`barrier` | VPUIP Barrier

### `VPUIP.DeclareBarrier` (vpux::VPUIP::DeclareBarrierOp)

VPUIP Barrier value static declaration


Syntax:

```
operation ::= `VPUIP.DeclareBarrier` attr-dict `->` type(results)
```


#### Results:

| Result | Description |
| :----: | ----------- |
`barrier` | VPUIP Barrier

### `VPUIP.DeclareTensor` (vpux::VPUIP::DeclareTensorOp)

MemRef value declaration


Syntax:

```
operation ::= `VPUIP.DeclareTensor` $location (`,` $offset^)? attr-dict `->` type(results)
```


#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
`location` | MemoryLocationAttr | Values indicating which type of memory a tensor resides in
`offset` | ::mlir::IntegerAttr | 64-bit signless integer attribute

#### Results:

| Result | Description |
| :----: | ----------- |
`memory` | memref of any type values

### `VPUIP.End` (vpux::VPUIP::EndOp)

End indicator for VPUIP Graph user inputs/outputs info lists


Syntax:

```
operation ::= `VPUIP.End` attr-dict
```


### `VPUIP.Graph` (vpux::VPUIP::GraphOp)

The root object for the VPUIP Execution Graph


Syntax:

```
operation ::= `VPUIP.Graph` attr-dict
              $identifier `at` $entryPoint
              `options` `:` $options
              `resources` `:` $resources
              `inputsInfo` `:` $inputsInfo
              `outputsInfo` `:` $outputsInfo
```


#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
`identifier` | ::mlir::StringAttr | string attribute
`entryPoint` | ::mlir::FlatSymbolRefAttr | flat symbol reference attribute
`options` | ExecutionFlagAttr | Each of these enums' presence informs how the current schedule is configured
`resources` | vpux::VPUIP::ResourcesAttr | DictionaryAttr with field(s): 'upa_shaves', 'nce2_blocks', 'upa_shared_cmx', 'nn_cmx_per_slice', 'nn_cmx_slice_amount', 'ddr_scratch', 'csram_storage' (each field having its own constraints)

### `VPUIP.SoftMaxUPA` (vpux::VPUIP::SoftMaxUPAOp)

SoftMax UPA SHAVE kernel


Syntax:

```
operation ::= `VPUIP.SoftMaxUPA` attr-dict
              (`inputs` `(` $inputTensors^ `:` type($inputTensors) `)`)?
              (`outputs` `(` $outputTensors^ `:` type($outputTensors) `)`)?
              (`waits` `(` $waitBarriers^ `:` type($waitBarriers) `)`)?
              (`updates` `(` $updateBarriers^ `:` type($updateBarriers) `)`)?
```


#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
`axisInd` | ::mlir::IntegerAttr | 32-bit signless integer attribute
`maxShaves` | ::mlir::IntegerAttr | 32-bit signless integer attribute
`isTrailingSWLayer` | ::mlir::UnitAttr | unit attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
`inputTensors` | memref of 16-bit float values
`outputTensors` | memref of 16-bit float values
`waitBarriers` | VPUIP Barrier
`updateBarriers` | VPUIP Barrier

### `VPUIP.TensorInfo` (vpux::VPUIP::TensorInfoOp)

Information about user inputs/outputs tensors


Syntax:

```
operation ::= `VPUIP.TensorInfo` attr-dict
              $name `,` $precision `,` $layout
```


#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
`name` | ::mlir::StringAttr | string attribute
`precision` | ::mlir::TypeAttr | any type attribute
`layout` | ::mlir::AffineMapAttr | AffineMap attribute

### `VPUIP.UPADMA` (vpux::VPUIP::UPADMAOp)

UPA DMA task


Syntax:

```
operation ::= `VPUIP.UPADMA` attr-dict
              (`inputs` `(` $inputTensors^ `:` type($inputTensors) `)`)?
              (`outputs` `(` $outputTensors^ `:` type($outputTensors) `)`)?
              (`waits` `(` $waitBarriers^ `:` type($waitBarriers) `)`)?
              (`updates` `(` $updateBarriers^ `:` type($updateBarriers) `)`)?
```


#### Operands:

| Operand | Description |
| :-----: | ----------- |
`inputTensors` | memref of any type values
`outputTensors` | memref of any type values
`waitBarriers` | VPUIP Barrier
`updateBarriers` | VPUIP Barrier

## Type definition

### `VPUIP_BarrierType` (BarrierType)

This object represents closely a Barrier in the device

