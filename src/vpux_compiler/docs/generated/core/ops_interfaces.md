<!-- Autogenerated by mlir-tblgen; don't manually edit -->
# OpInterface definitions
## CTCGreedyDecoderLayerInterface (`CTCGreedyDecoderLayerInterface`)

Interface for CTCGreedyDecoder Layer
### Methods:
## CTCGreedyDecoderSeqLenLayerInterface (`CTCGreedyDecoderSeqLenLayerInterface`)

Interface for CTCGreedyDecoderSeqLen Layer
### Methods:
## ClampLayerInterface (`ClampLayerInterface`)

Interface for Clamp Layer
### Methods:
#### `input`

```c++
mlir::Value input();
```
Get the source Value for the Operation#### `output`

```c++
mlir::Value output();
```
Get destination Value for the Operation#### `getMin`

```c++
float getMin();
```
Get the lower bound of the <min;max> range
NOTE: This method *must* be implemented by the user.

#### `getMax`

```c++
float getMax();
```
Get the upper bound of the <min;max> range
NOTE: This method *must* be implemented by the user.

## ConstantInterface (`ConstantInterface`)

Interface for Constant Tensor/Buffer Declaration
### Methods:
#### `getContent`

```c++
vpux::ConstContentAttr getContent();
```
Get the constant content from the Operation
NOTE: This method *must* be implemented by the user.

#### `getContentType`

```c++
mlir::ShapedType getContentType();
```
Get the content Type
NOTE: This method *must* be implemented by the user.

#### `output`

```c++
mlir::Value output();
```
Get resulting Value for the Operation#### `getActualType`

```c++
mlir::ShapedType getActualType();
```
Get the output Value Type
NOTE: This method *must* be implemented by the user.

#### `convertTo`

```c++
void convertTo(MutableArrayRef<char> buf);
```
Convert Content to actual output Type
NOTE: This method *must* be implemented by the user.

## ConvertLayerInterface (`ConvertLayerInterface`)

Interface for Convert Layer
### Methods:
#### `input`

```c++
mlir::Value input();
```
Get the source Value for the Operation#### `inputType`

```c++
mlir::ShapedType inputType();
```
Get the input Type
NOTE: This method *must* be implemented by the user.

#### `output`

```c++
mlir::Value output();
```
Get destination Value for the Operation#### `outputType`

```c++
mlir::ShapedType outputType();
```
Get the output Type
NOTE: This method *must* be implemented by the user.

## ConvolutionLayerInterface (`ConvolutionLayerInterface`)

Interface for Convolution Layer
### Methods:
#### `input`

```c++
mlir::Value input();
```
Get the source Value for the Operation#### `filter`

```c++
mlir::Value filter();
```
Get the source Value for the Operation#### `bias`

```c++
mlir::Value bias();
```
Get the source Value for the Operation#### `output`

```c++
mlir::Value output();
```
Get destination Value for the Operation## DetectionOutputLayerInterface (`DetectionOutputLayerInterface`)

Interface for DetectionOutput Layer
### Methods:
## EltwiseLayerInterface (`EltwiseLayerInterface`)

Interface for Eltwise Layer
### Methods:
## EluLayerInterface (`EluLayerInterface`)

Interface for Elu Layer
### Methods:
#### `input`

```c++
mlir::Value input();
```
Get the source Value for the Operation#### `output`

```c++
mlir::Value output();
```
Get destination Value for the Operation#### `getX`

```c++
float getX();
```
Get the scale for the negative factor
NOTE: This method *must* be implemented by the user.

## FullyConnectedLayerInterface (`FullyConnectedLayerInterface`)

Interface for FullyConnected Layer
### Methods:
## GRNLayerInterface (`GRNLayerInterface`)

Interface for GRN Layer
### Methods:
#### `input`

```c++
mlir::Value input();
```
Get the source Value for the Operation#### `output`

```c++
mlir::Value output();
```
Get destination Value for the Operation#### `getBias`

```c++
float getBias();
```
Get the bias added to the variance
NOTE: This method *must* be implemented by the user.

## LayerInterface (`LayerInterface`)

Base interface for Layer Operation
### Methods:
#### `getInputs`

```c++
vpux::SmallVector<mlir::Value> getInputs();
```
Get all inputs (tensors or memory buffers) for the Operation#### `getOutputs`

```c++
vpux::SmallVector<mlir::Value> getOutputs();
```
Get the outputs (tensors or memory buffers) for the Operation## LeakyReluLayerInterface (`LeakyReluLayerInterface`)

Interface for LeakyRelu Layer
### Methods:
#### `input`

```c++
mlir::Value input();
```
Get the source Value for the Operation#### `output`

```c++
mlir::Value output();
```
Get destination Value for the Operation#### `getNegativeSlope`

```c++
float getNegativeSlope();
```
Get the source Value for the Operation
NOTE: This method *must* be implemented by the user.

## PReluLayerInterface (`PReluLayerInterface`)

Interface for PRelu Layer
### Methods:
#### `input`

```c++
mlir::Value input();
```
Get the source Value for the Operation#### `negative_slope`

```c++
mlir::Value negative_slope();
```
Get the source Value for the Operation#### `output`

```c++
mlir::Value output();
```
Get destination Value for the Operation## ROIPoolingLayerInterface (`ROIPoolingLayerInterface`)

Interface for ROIPooling Layer
### Methods:
#### `getOutputSize`

```c++
mlir::ArrayAttr getOutputSize();
```
Get the source Value for the Operation
NOTE: This method *must* be implemented by the user.

#### `getSpatialScale`

```c++
float getSpatialScale();
```
Get the source Value for the Operation
NOTE: This method *must* be implemented by the user.

## ScaleShiftLayerInterface (`ScaleShiftLayerInterface`)

Interface for ScaleShift Layer
### Methods:
## SoftMaxLayerInterface (`SoftMaxLayerInterface`)

Interface for SoftMax Layer
### Methods:
#### `input`

```c++
mlir::Value input();
```
Get the source Value for the Operation#### `inputType`

```c++
mlir::ShapedType inputType();
```
Get the input Type
NOTE: This method *must* be implemented by the user.

#### `output`

```c++
mlir::Value output();
```
Get destination Value for the Operation#### `outputType`

```c++
mlir::ShapedType outputType();
```
Get the output Type
NOTE: This method *must* be implemented by the user.

#### `axisInd`

```c++
uint32_t axisInd();
```
Get the axis index in logical dimensions#### `getAxisDim`

```c++
vpux::Dim getAxisDim();
```
Get the axis parameter in logical dimensions
NOTE: This method *must* be implemented by the user.

## SwishLayerInterface (`SwishLayerInterface`)

Interface for Swish Layer
### Methods:
## TransposeLayerInterface (`TransposeLayerInterface`)

Interface for Transpose Layer
### Methods:
