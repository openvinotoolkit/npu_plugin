<!-- Autogenerated by mlir-tblgen; don't manually edit -->
# TypeInterface definitions
## GroupedTypeInterface (`GroupedTypeInterface`)

Interface representing types that are composed of multiple types.

### Methods:
## NDTypeInterface (`NDTypeInterface`)

Interface for extracting shape, element type, layout, memory space and other tensor related properties from the type.

### Methods:
#### `getShape`

```c++
vpux::ShapeRef getShape();
```
Returns the shape of the underlying type
NOTE: This method *must* be implemented by the user.

#### `getMemShape`

```c++
vpux::MemShape getMemShape();
```
Returns the memory shape of the underlying type
NOTE: This method *must* be implemented by the user.

#### `hasRank`

```c++
bool hasRank();
```
Returns true if the underlying type is ranked
NOTE: This method *must* be implemented by the user.

#### `getRank`

```c++
int64_t getRank();
```
Returns the rank of the underlying type
NOTE: This method *must* be implemented by the user.

#### `getNumElements`

```c++
int64_t getNumElements();
```
Returns the number of elements of the underlying type
NOTE: This method *must* be implemented by the user.

#### `getElementType`

```c++
mlir::Type getElementType();
```
Returns the element type of the underlying type
NOTE: This method *must* be implemented by the user.

#### `getDimsOrder`

```c++
vpux::DimsOrder getDimsOrder();
```
Returns the layout of the underlying type
NOTE: This method *must* be implemented by the user.

#### `getMemSpace`

```c++
vpux::IndexedSymbolAttr getMemSpace();
```
Returns the memory space of the underlying type
NOTE: This method *must* be implemented by the user.

#### `getMemoryKind`

```c++
vpux::VPU::MemoryKind getMemoryKind();
```
Returns the memory kind of the underlying type
NOTE: This method *must* be implemented by the user.

#### `getStrides`

```c++
vpux::Strides getStrides();
```
Returns the strides of the underlying type
NOTE: This method *must* be implemented by the user.

#### `getMemStrides`

```c++
vpux::MemStrides getMemStrides();
```
Returns the memory strides of the underlying type
NOTE: This method *must* be implemented by the user.

#### `getElemTypeSize`

```c++
vpux::Bit getElemTypeSize();
```
Returns the element type size of the underlying type
NOTE: This method *must* be implemented by the user.

#### `getTotalAllocSize`

```c++
vpux::Byte getTotalAllocSize();
```
Returns the total allocation size of the underlying type
NOTE: This method *must* be implemented by the user.

#### `getCompactAllocSize`

```c++
vpux::Byte getCompactAllocSize();
```
Returns the compact allocation size of the underlying type
NOTE: This method *must* be implemented by the user.

#### `changeShape`

```c++
vpux::NDTypeInterface changeShape(vpux::ShapeRef shape);
```
Returns the type with the new shape
NOTE: This method *must* be implemented by the user.

#### `changeElemType`

```c++
vpux::NDTypeInterface changeElemType(mlir::Type elemType);
```
Returns the type with the new element type
NOTE: This method *must* be implemented by the user.

#### `changeShapeElemType`

```c++
vpux::NDTypeInterface changeShapeElemType(vpux::ShapeRef shape, mlir::Type elemType);
```
Returns the type with the new shape and element type
NOTE: This method *must* be implemented by the user.

#### `changeDimsOrder`

```c++
vpux::NDTypeInterface changeDimsOrder(vpux::DimsOrder order);
```
Returns the type with the new order
NOTE: This method *must* be implemented by the user.

#### `changeMemSpace`

```c++
vpux::NDTypeInterface changeMemSpace(vpux::IndexedSymbolAttr memSpace);
```
Returns the type with the new memory space
NOTE: This method *must* be implemented by the user.

#### `changeStrides`

```c++
vpux::NDTypeInterface changeStrides(vpux::StridesRef strides);
```
Returns the type with the new strides
NOTE: This method *must* be implemented by the user.

#### `changeTypeComponents`

```c++
vpux::NDTypeInterface changeTypeComponents(vpux::TypeComponents typeComponents);
```
Returns the type with the new values for the given components
NOTE: This method *must* be implemented by the user.

#### `extractDenseTile`

```c++
vpux::NDTypeInterface extractDenseTile(vpux::ShapeRef tileOffsets, vpux::ShapeRef tileShape);
```
Returns a tile from the type
NOTE: This method *must* be implemented by the user.

#### `extractViewTile`

```c++
vpux::NDTypeInterface extractViewTile(vpux::ShapeRef tileOffsets, vpux::ShapeRef tileShape, vpux::ShapeRef tileElemStrides);
```
Returns a view tile from the type
NOTE: This method *must* be implemented by the user.

#### `eraseTiledInfo`

```c++
vpux::NDTypeInterface eraseTiledInfo();
```
Returns the type without strides information
NOTE: This method *must* be implemented by the user.

#### `pad`

```c++
vpux::NDTypeInterface pad(vpux::ShapeRef padBefore, vpux::ShapeRef padAfter);
```
Returns the padded type
NOTE: This method *must* be implemented by the user.

